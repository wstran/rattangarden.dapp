const theme_id = "rattan-garden";

const themeConfig = {
    locale: 'en',
    theme: 'light',
};

const idb_name = 'rattan-garden';

const idb_objects = ['images'];

export { theme_id, themeConfig, idb_name, idb_objects };
